task_type: hip2hip
source_file_path:
- src/mhc_head.cu
harness_path: test_mhc_head_hip_harness.py
compile_command:
- python3 test_mhc_head_hip_harness.py --correctness
correctness_command:
- python3 test_mhc_head_hip_harness.py --correctness
performance_command:
- python3 test_mhc_head_hip_harness.py --full-benchmark
target_kernel_functions:
- hc_head_baseline_kernel
- hc_head_cuda_launch
prompt:
  instructions: Optimize the CUDA/HIP hc_head baseline kernel for Hygon K500SM_AI
    DCU gfx928. The source is compiled by hipcc with -x hip. Preserve the formula
    hidden = sum_c head_mix[c] * residual[c], FP32 accumulation, and BF16 output
    correctness for all harness shapes.
